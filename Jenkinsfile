pipeline {
    agent any
    stages {
        stage('TEST') {
            steps {
                echo 'Hello World'
                git url 'https://github.com/ImadElAlami/jenkinsExam.git'
            }
        }
    }
}
